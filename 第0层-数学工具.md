# 第零层：数学工具

> 电力系统的语言是数学。没有这层工具，后面所有分析都只能停留在"感觉"层面。
>
> 你的目标不是"学会数学证明"，而是**能熟练运用这些工具分析电气量**。

---

## 目录

- [0.1 相量与复数](#01-相量与复数)
  - [从正弦量到相量](#从正弦量到相量)
  - [相量的两种表示与运算规则](#相量的两种表示与运算规则)
  - [核心推导：微分方程 → 代数方程](#核心推导微分方程--代数方程)
  - [理解 di/dt 和 ∫i dt](#理解-didt-和-i-dt)
  - [谐振的直觉理解](#谐振的直觉理解)
  - [与 FTU 工作的关联](#与-ftu-工作的关联)
- [0.2 对称分量法](#02-对称分量法)
  - [旋转因子 a 的完整推导](#旋转因子-a-的完整推导)
  - [数学定义与变换矩阵](#数学定义与变换矩阵)
  - [手算实例](#手算实例)
  - [关键性质](#关键性质)
- [0.3 标幺制](#03-标幺制)
- [附录：为什么电网用正弦波](#附录为什么电网用正弦波)
- [综合代码项目](#综合代码项目)
- [本层检验标准](#本层检验标准)
- [参考答案](#参考答案)

---

## 0.1 相量与复数

### 从正弦量到相量

交流电力系统中，电压和电流都是正弦波：

```
u(t) = Um · sin(ωt + φ)
```

如果直接用正弦函数计算功率、阻抗，需要求解微分方程，非常繁琐。

**相量的核心思想：** 用复数来表示正弦量，把微分方程变成代数方程。

```
u(t) = Um · sin(ωt + φ)
    ↓  用欧拉公式 e^(jθ) = cosθ + j·sinθ
u(t) = Im[Um · e^(j(ωt+φ))]
    ↓  去掉时间因子 e^(jωt)，只保留幅值和相位信息
U·   = U∠φ     （U 是有效值 = Um/√2）
```

**这就是相量：** 一个复常数，模 = 有效值，幅角 = 初相角。

### 相量的两种表示与运算规则

| 形式 | 表达式 | 适用场景 |
|------|--------|---------|
| 极坐标 | U∠φ | 直观看出幅值和相位，潮流计算 |
| 直角坐标 | a + jb | 加减运算方便，短路计算 |

**互相转换：**

```
a = U·cosφ        U = √(a² + b²)
b = U·sinφ        φ = arctan(b/a)   // 注意象限
```

**加减法：** 直角坐标形式，实部虚部分别加减

```
U1· + U2· = (a1 + a2) + j(b1 + b2)
```

**乘除法：** 极坐标形式，模相乘除，幅角相加减

```
U1· × U2· = U1·U2 ∠(φ1 + φ2)
U1· ÷ U2· = U1/U2 ∠(φ1 - φ2)
```

### 核心推导：微分方程 → 代数方程

> 这是整个相量理论最关键的一步。

#### 场景：RLC 串联电路

```
    R        L        C
───/\/\/\───○○○───|(───
+                         |
u(t)                      |
-                         |
──────────────────────────
```

根据基尔霍夫电压定律（KVL）：

```
u(t) = R·i(t) + L·di(t)/dt + (1/C)·∫i(t)dt
```

这是**微分-积分方程**。每换一个电路结构就要重新解，非常麻烦。

#### 关键观察

如果 u(t) 和 i(t) 都是**同频率的正弦波**，那么：

```
对正弦波求导  =  幅值乘 ω，相位超前 90°
对正弦波积分  =  幅值除以 ω，相位滞后 90°

u(t) = U·sin(ωt)
du/dt = U·ω·sin(ωt + 90°)
∫u dt = -(U/ω)·sin(ωt - 90°)
```

**求导和积分，在正弦波上只是幅值和相位的变化。**

#### 相域下的对应

在复数世界里：

```
j = 1∠90°    → 乘以 j = 幅值不变，相位超前 90°
-j = 1∠-90°  → 除以 j = 幅值不变，相位滞后 90°
```

| 时域对正弦量的操作 | 相域对应的运算 |
|-----------------|--------------|
| d/dt（求导） | × jω |
| ∫dt（积分） | ÷ jω = × (-j/ω) |

#### 回到 RLC 电路，用相量

逐项转换：

- u(t) → U·（电压相量）
- i(t) → I·（电流相量）
- R·i(t) → R·I·
- L·di(t)/dt → jωL·I·（求导 → × jω）
- (1/C)·∫i(t)dt → I·/(jωC)（积分 → ÷ jω）

全部代入：

```
U· = R·I· + jωL·I· + I·/(jωC)
   = (R + jωL + 1/(jωC)) · I·
   = [R + j(ωL - 1/(ωC))] · I·
   = Z · I·
```

**Z = R + j(ωL - 1/(ωC)) 就是复阻抗。**

#### 结果

```
时域：u(t) = R·i(t) + L·di/dt + (1/C)·∫i dt    ← 微分方程
相域：U· = Z · I·                               ← 代数方程
```

想求电流？直接除法：`I· = U· / Z`。想求某元件电压？直接乘法：`U_L· = jωL·I·`。

**全部是复数的乘除法，没有微分，没有积分。**

| | 时域（正弦函数） | 相域（复数） |
|---|---|---|
| 电阻 | R | R |
| 电感 | L·di/dt | jωL |
| 电容 | (1/C)·∫dt | 1/(jωC) |
| 电路方程 | 微分方程 | 代数方程 |

**相量的本质：利用复数运算规则，把对正弦量的微积分运算变成了乘法。**

### 理解 di/dt 和 ∫i dt

电阻的公式 `u = R·i` 好懂——电流流过去立刻产生压降，像水管摩擦。电感和电容不是这样，它们带"记忆"。

**电感：反抗变化，有"惯性"**

```
u_L = L × di/dt
```

电感在乎的不是电流多大，而是电流**变得多快**。`di/dt` 就是电流的斜率——曲线越陡，变化越剧烈。

```
正弦波一个周期内：
  过零点：电流=0，但变化最快 → u_L 最大
  峰值：  电流最大，但变化=0  → u_L = 0
```

电感就像飞轮——匀速转时没阻力，但你想让它加速或减速，它就给你一股反抗的力。电流稳定时，电感像一根导线。

**电容：积累后响应，有"容量"**

```
u_C = (1/C) × ∫i dt
```

电容在乎的是电流**存了多少进去**。`∫i dt` 是从开始到现在所有电流累积的总电荷——曲线下面的面积。

```
正弦波一个周期内：
  电流为正：往里灌电荷 → 电压慢慢上升
  电流=0：  不灌也不放 → 电压停在当前值
  电流为负：往外抽电荷 → 电压慢慢下降
```

电容就像水管里绷了张橡皮膜——水流进去撑开膜，撑得越大回弹力（电压）越大。电流停了，膜停在当前位置，电压保持不动。

**电感 vs 电容，镜像关系**

```
      电感（飞轮）           电容（橡皮膜）
关心   di/dt（变化快慢）      ∫i dt（累积多少）
i=0时  u 最大（变得最快）     u=0（还没存东西）
i最大时 u=0（不变了）         u 慢慢涨到最大
能量   在磁场中              在电场中
```

> 电阻是"即时反应"，电感和电容都带"记忆"——电感记得电流在往哪跑，电容记得灌了多少电进去。

### 谐振的直觉理解

把电感和电容放在同一个电路里，就会发生**谐振**。

#### L 和 C 在打架

在正弦波下，电感和电容的特性刚好相反：

```
电感：i=0 时 u 最大，i 最大时 u=0
电容：i=0 时 u=0，i 最大时 u 最大
```

`u_L` 和 `u_C` 的相位永远差 180°，互相打架。

#### 秋千类比

```
推力 → 电源电压
摆动 → 电流
重力 → 电容（往回拉）
惯性 → 电感（停不下来）
```

频率不对：推的节奏和秋千不匹配，费劲。**频率刚好（谐振）：** 推的时刻正好是秋千荡过去的时刻，轻轻一推就越来越大。电源几乎不用出力，只需克服摩擦。

#### 能量视角

```
               磁场能量              电场能量
        ┌───   电感 L   ──→   电容 C   ──┐
        │   (电流在流动)      (电压在蓄满)   │
        │                                  │
        └────────←──── 电容 C ──────────┘
                     (电压放出，电流反向)
```

能量从 L 流到 C，再从 C 流回 L，来回振荡。电源只需补充电阻消耗的那一小部分。

谐振频率由 L、C 本身决定：

```
ω₀ = 1/√(LC)        f₀ = 1/(2π√LC)
```

#### 谐振为什么会有危害

谐振时 L 和 C 互相抵消，电源只看到电阻，电流最大——听起来很好。问题是 L 和 C **各自**在承受什么。

数值例子：R=1Ω，L=10mH，C=10μF，电源 10V。

```
谐振频率 f₀ = 1/(2π × √(0.01 × 0.00001)) ≈ 503Hz

在 503Hz 下：
  X_L = 2π × 503 × 0.01 = 31.6Ω
  X_C = 1/(2π × 503 × 0.00001) = 31.6Ω

谐振时总阻抗 Z = R = 1Ω
电流 I = 10V / 1Ω = 10A

电感电压 u_L = 10A × 31.6Ω = 316V    ← 电源才 10V！
电容电压 u_C = 10A × 31.6Ω = 316V    ← 绝缘可能击穿

但 u_L + u_C = 316∠90° + 316∠−90° = 0  ← 对外抵消
电源只看到 u_R = 10A × 1Ω = 10V        ← 毫不知情
```

**串联谐振 → 过电压。** L 和 C 各自承受 Q 倍电源电压（Q = X/R = 31.6）。

**并联谐振 → 过电流。** 回路内部电流是端口电流的 Q 倍，电容器鼓包。

真实事故：某变电站 10kV 母线，PT 铁芯电感和对地电容在操作冲击下串联谐振 → PT 一次侧电流暴增 → PT 爆炸，母线跳闸。

#### 什么时候会发生

同一组 L、C，在 50Hz 工频下未必谐振：

```
50Hz 时：X_L = 3.14Ω，X_C = 318Ω → 不谐振
```

但系统里不只 50Hz：

```
变频器 → 5 次谐波（250Hz）、7 次（350Hz）、11 次（550Hz）
电容器合闸 → 暂态振荡，频率由 L、C 本身决定
PT 铁芯饱和 → 电感突变，恰好和 C 匹配
```

> 设计时要**做谐波阻抗扫描**——确保 L、C 组合不会在常见谐波频率上谐振。

### 与 FTU 工作的关联

你在 FTU 里算功率，就是在做相量运算：

```
P = U·I·cosφ       Q = U·I·sinφ
```

复数形式：

```
S· = U· × I·*      （I·* 是电流相量的共轭）
P = Re(S·)          // 有功 = 实部
Q = Im(S·)          // 无功 = 虚部
```

```
// FTU 伪代码
typedef struct {
    float real;
    float imag;
} complex_t;

complex_t phasor_power(complex_t U, complex_t I) {
    // S = U × I*
    complex_t S;
    S.real = U.real * I.real + U.imag * I.imag;
    S.imag = U.imag * I.real - U.real * I.imag;
    return S;   // real = P, imag = Q
}
```

**你每天在代码里算功率，就是在用相量理论。**

### Feynman 检验 1

> "相量就是把交流电的幅值和相位打包成一个复数，这样分析电路时就不用解微分方程了。"

试着把这句话展开成两三句话，写在下面。写出来就算过。

### 练习题

1. 已知 u(t) = 311·sin(100πt + 30°)，写出其相量形式（有效值相量）。
2. 两个相量：U1· = 220∠0°，U2· = 220∠-120°，求 U1· + U2· 的极坐标形式。
3. Z = 4 + j3Ω，I· = 50∠-30°A，求电压相量 U·。

题做完才能往下走。

---

## 0.2 对称分量法

### 旋转因子 a 的完整推导

> a 是对称分量法的核心。记住几何意义就够了：**a 就是把一个相量在复平面上逆时针转 120°。**

#### 几何直觉

在复平面上，乘以 e^(jθ) 就是逆时针旋转 θ：

```
(r∠φ) × (1∠θ) = r∠(φ + θ)    ← 模不变，幅角加 θ
```

所以 e^(j120°) 就是把任意相量**逆时针旋转 120°**。

#### 推导 a 的直角坐标值

```
a = e^(j120°) = cos120° + j·sin120°

120° 在第二象限，参考角 = 180° - 120° = 60°：
  cos120° = -cos60°  = -1/2      （第二象限 x 为负）
  sin120° = +sin60°  = +√3/2     （第二象限 y 为正）

∴ a = -1/2 + j·√3/2
```

验证模：√((-1/2)² + (√3/2)²) = √(1/4 + 3/4) = 1 ✓

#### 推导 a²

极坐标（直观）：

```
a² = 1∠120° × 1∠120° = 1∠240°
```

直角坐标（验证）：

```
a² = (-1/2 + j·√3/2)² = -1/2 - j·√3/2
```

与 cos240° + j·sin240° 一致（240° 在第三象限，cos 和 sin 都为负）。

#### 推导 a³ = 1

```
a³ = 1∠120° × 1∠120° × 1∠120° = 1∠360° = 1
```

**三次旋转 120° = 旋转 360° = 回到原点。** 代数上，a、a²、a³=1 就是 x³=1 的三个复数根。

#### 推导 1 + a + a² = 0

```
1 + a + a²
= 1 + (-1/2 + j·√3/2) + (-1/2 - j·√3/2)

实部：1 + (-1/2) + (-1/2) = 0
虚部：0 + j·√3/2 + (-j·√3/2) = 0

∴ 1 + a + a² = 0
```

**几何意义：三个大小相等、方向互差 120° 的向量，矢量和为零。**

```
                     ↑ 虚轴
                     │
          a          │
        ·(-0.5, 0.866)
        ╱            │
       ╱ 120°        │
      ●──────────────●──→ 实轴
       ╲             1 (1∠0°)
        ╲            │
         a²          │
        ·(-0.5,-0.866)
                     ↓
```

这就是**三相对称时电压之和为零**的数学本质。

#### 小结：四个必须记住的值

```
a  = e^(j120°)  = -1/2 + j·√3/2  = 1∠120°     逆时针旋转 120°
a² = e^(j240°)  = -1/2 - j·√3/2  = 1∠−120°    顺时针旋转 120°
a³ = e^(j360°)  = 1              = 1∠0°       转一圈回原点

1 + a + a² = 0    三个均衡矢量互相抵消
```

### 数学定义与变换矩阵

电网正常运行时三相对称：`Ua + Ub + Uc = 0`。发生故障时不再对称。对称分量法就是把**不对称的三相量分解为三组对称的相量**。

```
[I0]      [1   1    1 ] [Ia]
[I1] = 1/3[1   a   a²] [Ib]
[I2]      [1  a²   a ] [Ic]
```

矩阵中 a 的作用，一行一行看：

**零序行 [1, 1, 1]：** 三相直接相加取平均，没有旋转。

```
I0 = (Ia + Ib + Ic) / 3
```

**正序行 [1, a, a²]：** 从 A 相的视角"对齐" B 相和 C 相。B 相天然滞后 A 相 120°（即 a²），用 a 乘 Ib 把它转回来；C 相用 a² 乘 Ic。

```
正序（a → b → c）：Ib1 = a²·Ia1，Ic1 = a·Ia1
```

**负序行 [1, a², a]：** 旋转方向相反，相序反过来（a → c → b）。

```
负序（a → c → b）：Ib2 = a·Ia2，Ic2 = a²·Ia2
```

**三组对称分量：**

| 分量 | 物理含义 | 三相特征 |
|------|---------|---------|
| 正序 (I1) | 正常能量传输 | A 超前 B 120°，B 超前 C 120° |
| 负序 (I2) | 不对称故障 | A 滞后 B 120°（反向旋转） |
| 零序 (I0) | 接地故障 | 三相幅值相位完全相同 |

**逆变换：**

```
Ia = I1 + I2 + I0
Ib = a²·I1 + a·I2 + I0
Ic = a·I1 + a²·I2 + I0
```

### 手算实例

缺一相（C 相断开）的三相电流：

```
Ia = 1∠0° A
Ib = 1∠−120° A
Ic = 0 A
```

#### 第一步：极坐标 → 直角坐标

```
Ia = 1∠0°       →  1.0   + j0.0
Ib = 1∠−120°    →  −0.5  − j0.866
Ic = 0          →  0     + j0

公式：x = r·cosφ，y = r·sinφ
逆公式：r = √(x² + y²)，φ = arctan(y/x)
```

#### 第二步：向量相加

```
Ia + Ib + Ic = (1.0 − 0.5) + j(0.0 − 0.866) = 0.5 − j0.866
```

> 向量相加是实部、虚部分开加，不是幅值直接加。两个 1A 的向量加起来幅值只有 1A，因为方向不同。

#### 第三步：乘 1/3，得零序

```
I₀ = ⅓(0.5 − j0.866) = 0.1667 − j0.2887
r = √(0.1667² + 0.2887²) = 0.333
φ = arctan(−0.2887 / 0.1667) = −60°

I₀ = 0.333∠−60° A
```

#### 第四步：正序

```
a·Ib = 1∠120° × 1∠−120° = 1∠0°     ← 旋转抵消

I₁ = ⅓ [1∠0° + 1∠0° + 0] = 0.667∠0° A
```

#### 第五步：负序

```
a²·Ib = 1∠−120° × 1∠−120° = 1∠−240°
I₂ = ⅓ [1∠0° + 1∠−240°]
   = ⅓ [(1 + j0) + (−0.5 + j0.866)]
   = ⅓ (0.5 + j0.866)
   = 0.333∠60° A
```

#### 结果

| 分量 | 幅值 | 相位 |
|------|------|------|
| 零序 I₀ | 0.333 A | −60° |
| 正序 I₁ | 0.667 A | 0° |
| 负序 I₂ | 0.333 A | +60° |

#### 验证

```
Ia = I₀ + I₁ + I₂
0.333∠−60° + 0.667∠0° + 0.333∠60°
= (0.1667−j0.2887) + (0.667+j0) + (0.1667+j0.2887)
= 1.0 + j0.0 = 1∠0°  ✓
```

> **直觉：** 缺一相时，正序降到 2/3，零序和负序各占 1/3，相位关于正序对称（−60° 和 +60°）。

### 关键性质

**1. 三相对称时零序为零**

如果 Ia + Ib + Ic = 0，则 I0 = 0。正常电网没有零序分量。

**2. 零序的出现 = 不对称或接地**

零序保护能检测接地故障的数学根源。

**3. 负序的出现 = 不对称故障**

如两相短路、单相断线。

### Feynman 检验 2

> "不平衡的三相可以拆成三组对称分量：正序像正常的旋转磁场，负序像反向旋转的磁场，零序像三个相等的分量同方向振动。"

试着用自己的话说出来。

### 练习题

已知三相电流（有效值相量）：

```
Ia = 100∠0°
Ib = 50∠-150°
Ic = 30∠90°
```

求：

1. 零序电流 I0（复数形式）
2. 正序电流 I1（复数形式）
3. 判断这组电流中是否存在接地故障分量

提示：用公式算 I0 = (Ia + Ib + Ic) / 3

---

## 0.3 标幺制

### 为什么用标幺值

电网中有多个电压等级：

```
500kV → 220kV → 110kV → 10kV → 380V
```

每个电压等级下，同样一个阻抗的欧姆值代表不同的"电气意义"。直接用有名值计算非常麻烦。

**标幺制的思想：** 设定基准值，把实际值转换成相对值。

### 定义

```
标幺值 = 实际值 / 基准值      用 p.u.（per unit）表示
```

### 基准值的选择

通常先选两个基准值，其余两个由公式导出：

- 基准功率 S_base = 100MVA（约定俗成）
- 基准电压 U_base = 该电压等级的平均额定电压（如 10.5kV, 115kV, 230kV, 525kV）

**导出：**

```
Z_base = U_base² / S_base
I_base = S_base / (√3 · U_base)
```

### 不同电压等级间的换算

标幺值的最大优势：**标幺值可以直接跨电压等级运算。**

一段 10kV 线路的阻抗标幺值为 0.05 p.u.，折算到 110kV 侧时——有名值变了（基准阻抗不同），但标幺值不变。

### 典型计算示例

```
U_base = 110kV，S_base = 100MVA

Z_base = 110² / 100 = 121Ω
线路实际阻抗 Z = 12.1Ω → Z(p.u.) = 12.1 / 121 = 0.1 p.u.
```

### Feynman 检验 3

> "标幺值就是把实际值除以一个基准值，变成相对值。就像说'这根线阻抗是标准值的 0.1 倍'。好处是不同电压等级的电气量可以直接比较和运算。"

### 练习题

已知某 10kV 配电网线路：

- S_base = 100MVA
- U_base = 10.5kV
- 线路实际阻抗 Z = 0.5 + j0.8Ω
- 线路末端负载 S = 5 + j3MVA

求：

1. 该电压等级的 I_base 和 Z_base
2. 线路阻抗的标幺值
3. 负载功率的标幺值
4. 如果该线路短路电流有名值为 5kA，标幺值是多少？

---

## 附录：为什么电网用正弦波

> 不是"因为教材选了正弦波"，而是发电机的物理结构决定了它只能发出正弦波。

### 发电机物理产生正弦波

发电机本质上是**线圈在磁场里旋转**。线圈以角速度 ω 匀速旋转，线圈法线与磁场方向夹角 θ = ωt 随时间连续变化。

物理推到数学的四步：

```
洛伦兹力 F = q(v × B)       ← 最根本的物理定律
       ↓
直导线切割磁场：e = BLv      ← v ⊥ B 时的简化
       ↓
加入角度：e = BLv·sin(θ)     ← 只有垂直于 B 的速度分量才切割磁力线
       ↓
线圈旋转 θ = ωt：e(t) = BLv·sin(ωt)  ← 发电机天然产生正弦波
```

**θ = ωt 是连续旋转的角度，sin(ωt) 天然出现。** 发电机不需要任何额外电路就能产生正弦波——它是物理结构决定的。

### 为什么 3000 转/分 = 50Hz

公式 `n = 60f/p` 的推导：

```
转子每秒转 n_rps 圈，每圈经过 p 对磁极 → 每秒 p×n_rps 个正弦波 = f
∴ f = n_rps × p

单位换算 n_rpm = n_rps × 60 → f = (n_rpm/60) × p
移项：n_rpm = 60f/p

代入 f=50Hz：
  p=1 → n=3000rpm（火电汽轮机）
  p=2 → n=1500rpm（水电中速）
  p=4 → n=750rpm（大型水电）
```

**60 就是"分钟 → 秒"的单位换算系数。**

### CT 也是同一原理

| 形式 | 公式 | 场景 |
|------|------|------|
| 动生电动势 | e = BLv·sinθ | 发电机 — 导线动，磁场不动 |
| 变压器电动势 | e = N·dΦ/dt | CT/PT — 磁场变，线圈不动 |

你在 FTU 里处理 CT 采样，和发电机原理根上同一条定律——法拉第电磁感应。

### 谐波畸变

你 FTU 里采到的电压电流并不是完美正弦波：

```
u(t) = U1·sin(ωt) + U3·sin(3ωt) + U5·sin(5ωt) + ...
```

非线性负载（变频器、整流器、LED 驱动）"污染"了波形。但畸变是以"偏离正弦"来定义的——这恰恰证明原信号应该是正弦波。

---

## 综合代码项目

> 把以上三个工具实现成代码，才算真正落地。

### 项目要求

用 Python 或 C 实现以下功能：

**模块一：复数相量运算库**

```
- to_polar(real, imag) → (mag, angle)
- to_rect(mag, angle) → (real, imag)
- phasor_add(p1, p2) → p3
- phasor_mul(p1, p2) → p3
- phasor_div(p1, p2) → p3
- print_phasor(p)          // 格式化输出 U∠φ°
```

**模块二：对称分量法计算**

```
- symmetrical_components(Ia, Ib, Ic) → (I0, I1, I2)
- inverse_symmetrical(I0, I1, I2) → (Ia, Ib, Ic)
- fault_type_detect(Ia, Ib, Ic) → "AG" / "BC" / "ABC" / "normal"
```

**模块三：标幺值换算**

```
- calc_base_impedance(U_base, S_base) → Z_base
- calc_base_current(U_base, S_base) → I_base
- to_per_unit(actual_value, base_value) → pu_value
- from_per_unit(pu_value, base_value) → actual_value
```

### 验收方式

用练习题中的数据作为输入，运行代码，输出结果与手工计算对比。

---

## 本层检验标准

完成本层后，你应该能：

1. ✅ 在纸上写出任意正弦量的相量形式
2. ✅ 用复数运算计算两个相量的加减乘除
3. ✅ 用对称分量法从三相量中分解出零序分量
4. ✅ 计算任意电压等级下的基准值和标幺值
5. ✅ 写出相量运算库的核心函数

完成以上 5 条之后，进入第 1 层（物理本质）。

---

## 参考答案

### 0.1 练习题

1. Um = 311V，U = 311/√2 ≈ 220V，φ = 30° → U· = 220∠30°V

2. ```
   U1 + U2 = 220∠0° + 220∠-120°
            = 220 + 220(-1/2 - j·√3/2)
            = 220 - 110 - j·190.5
            = 110 - j·190.5
            = 220∠-60°V
   ```

3. Z = 4 + j3 = 5∠36.87°Ω
   U· = I· × Z = 50∠-30° × 5∠36.87° = 250∠6.87°V

### 0.2 练习题

```
I0 = (100∠0° + 50∠-150° + 30∠90°) / 3

100∠0° = 100 + j0
50∠-150° = 50(-√3/2 - j·1/2) = -43.3 - j25
30∠90° = 0 + j30

I0 = (100 - 43.3 + 0) + j(0 - 25 + 30) / 3
   = (56.7 + j5) / 3
   = 18.9 + j1.67
   = 18.97∠5.05°A

I0 ≠ 0，存在接地分量，初步判断存在接地故障。
```

### 0.3 练习题

1. ```
   I_base = S_base / (√3 · U_base) = 100 / (1.732 · 10.5) = 5.5kA
   Z_base = U_base² / S_base = 10.5² / 100 = 1.1025Ω
   ```

2. ```
   Z(p.u.) = (0.5 + j0.8) / 1.1025 = 0.453 + j0.726 p.u.
   ```

3. ```
   S(p.u.) = (5 + j3) / 100 = 0.05 + j0.03 p.u.
   ```

4. ```
   I(p.u.) = 5 / 5.5 = 0.909 p.u.
   ```
